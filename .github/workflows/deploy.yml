name: Deploy to Linode on push

on:
  push:
    branches:
      - staging        # change to whatever branch you want to auto-deploy

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout (not strictly needed for SSH-only deploy, but harmless)
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh/
          echo "${{ SEMA_ID_RSA }}" > ../private.key
          sudo chmod 600 ../private.key
          echo "${{ LINODE_HOST }}" > ~/.ssh/known_hosts
        shell: bash

      - name: Run deploy script on Linode
        run: |
          ssh -o StrictHostKeyChecking=yes -i ../private.key ${{ LINODE_USER }}@${{ LINODE_HOST }} "/home/deploy/deploy_myapp.sh"
